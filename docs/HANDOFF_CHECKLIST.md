# Handoff Checklist

- [ ] README explains import + credential setup
- [ ] `.env.example` added with expected key names
- [ ] Runbook includes pre-run and rollback steps
- [ ] CI validates baseline repo structure
- [ ] No secrets committed
